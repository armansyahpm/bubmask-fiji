# Documentation Archive

This folder is for local historical records, such as development journals and
working drafts.

The archived development journal is kept locally as:

```text
docs/archive/bubmask-fiji_old_version.md
```

It is intentionally excluded from the public GitHub release so the repository
stays focused on the publication-stage report, user guide, source code, and
reproducible lightweight metadata.
