# Runtime Results

BubMask writes new Fiji run outputs here by default.

Historical runs from the development phase were moved to:

```text
artifacts/results_archive_20260601
```

One run folder may remain here if it is open in Fiji, VS Code, or another local
program. Close any open file handles before moving it to the archive.
