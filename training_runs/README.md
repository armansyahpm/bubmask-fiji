# Training Runs

Use this folder for new local training jobs.

Historical training checkpoints and logs from the research-prototype phase were
moved to:

```text
artifacts/training_runs_archive_20260601
```

Training outputs are large and are ignored by git. Promote only selected model
packages into `models/` after validation and metadata review.
